# Configurable params
flowmon_key_path: /path/to/.ssh/id_rsa # SSH key with access to flowmon
flowmon_ssh_passphrase: TBA # password to the flowmon SSH key

warden_ca_path: /path/to/cacert.pem
warden_cert_path: /path/to/cert.pem
warden_key_path: /path/to/key.pem

neo4j_password: TBA # password to neo4j database(your choice)
flower_password: TBA # password to flower instance(your choice)

rtir_user: rest_client_user # username of rtir user
rtir_password: TBA # password for rtir user

shadowserver_user: TBA # username of shadowserver account
shadowserver_password: TBA # password for shadowserver account

shodan_api_key: TBA # api key with access to shodan API

